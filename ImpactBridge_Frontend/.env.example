# Base URL of the FastAPI backend.
# Leave empty for local development (the CRA dev server proxies /api calls to http://localhost:8000).
# In Vercel, set this to your deployed backend, e.g. https://impactbridge-api.up.railway.app
REACT_APP_API_URL=
